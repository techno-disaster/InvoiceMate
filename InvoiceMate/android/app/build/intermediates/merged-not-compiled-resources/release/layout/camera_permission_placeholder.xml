<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cameraPermissionView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/bottomBarBackgroundColor"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp"
    android:visibility="gone">

    <ImageView
        android:id="@+id/camera_permission_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:scaleType="center"
        android:src="?ui_scanning_no_permission"
        android:tint="?bottomBarButtonsColor"/>

    <TextView
        android:id="@+id/camera_permission_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:text="?camera_permission_placeholder_title"
        android:textSize="14sp"
        tools:text="Camera permission description"
        android:textColor="?bottomBarButtonsColor"/>

    <Button
        android:id="@+id/enableCameraBtn"
        style="@style/Scanbot.Toolbar.Button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="?camera_permission_placeholder_btn_text"
        android:textSize="14sp"
        android:textAllCaps="true"
        android:textColor="?bottomBarButtonsColor"
        tools:text="Enable camera permission" />

</LinearLayout>